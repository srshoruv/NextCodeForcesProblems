#include "bits/stdc++.h"
//#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'
#define fastio ios_base::sync_with_stdio(false); cin.tie(NULL);

int main() {
    fastio;

    int testCase;
    cin >> testCase;

    while (testCase--) 
    {

        int n;
        cin >> n;
        int sum = 0, x = 0;

        vector<int> v(n);

        for (int i = 0; i < n; i++) 
        	cin >> v[i];

        int i = 0;

        for (int k = 1; k <= 2 * n; k++)
        {
        	while(k)
        	{
        		if(v[i] <= 1)
        	}
        }



    }

    return 0;
}